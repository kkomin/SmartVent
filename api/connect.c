#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <curl/curl.h>

// api 응답 데이터 저장할 구조체 정의
struct Memory {
    char *response;     // 응답 데이터 저장할 문자열
    size_t size;        // 현재 데이터 크기
};

int main() {
    // 메모리 초기화

    // libcurl 초기화

    // api url 설정
    

    // 요청 옵션 설정


    // libcurl 종료

    return 0;
}